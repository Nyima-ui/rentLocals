name: Insert Scraped Listing

on:
  schedule:
    - cron: '0 0 * * 1,3,5,0'
  workflow_dispatch:

jobs:
  insert-listing:
    runs-on: ubuntu-latest
    steps:
      - name: Call insert API
        run: |
           curl -X GET